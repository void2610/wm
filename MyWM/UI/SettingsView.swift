import SwiftUI

// 設定画面。Phase 5 で本実装する。ホットキー記録 UI は KeyboardShortcuts 標準を使う想定。
struct SettingsView: View {
    var body: some View {
        Text("設定画面 (Phase 5 で実装)")
            .padding()
            .frame(width: 480, height: 320)
    }
}
